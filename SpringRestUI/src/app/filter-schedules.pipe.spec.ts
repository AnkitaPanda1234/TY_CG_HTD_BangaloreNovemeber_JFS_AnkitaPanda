import { FilterSchedulesPipe } from './filter-schedules.pipe';

describe('FilterSchedulesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSchedulesPipe();
    expect(pipe).toBeTruthy();
  });
});
